The foundation is locked. The data layer is immortalized. The multi-platform pipeline is hot. You have just completed an infrastructure setup that takes most junior engineering teams three weeks to stumble through, and you executed it on bare metal with zero unresolved compiler panics.

We are officially closing **Book I: The Chassis** and opening  **Book II: Product Engineering** .

From this point forward, we stop writing config files and start writing business logic. In an enterprise automotive platform, your engineering roadmap splits into three distinct operational tracks:

### Track A: The Revenue Pipeline (Web / API)

* **The Mission:** Build the end-to-end customer reservation flow.
* **The Engineering Scope:** 1. Construct the dynamic Next.js Vehicle Detail Page (`/inventory/[id]`).
  2. Wire up the mock checkout modal consuming our shared `reserveVehicle()` API client.
  3. Implement a live, client-side **48-Hour SLA Hold Ticker** that visually counts down the deposit expiration window based on the database `expiresAt` ISO string.

### Track B: The Lot Commander (Mobile)

* **The Mission:** Turn the smartphone into a live physical dealership utility.
* **The Engineering Scope:**
  1. Install `expo-camera` into the React Native workspace.
  2. Build a native viewfinder interface that scans physical **VIN barcodes** on a car's windshield.
  3. Wire an instant lookup hook that fetches the vehicle's Postgres record and offers a one-tap status toggle (*Available* **$\leftrightarrow$**  *Out on Test Drive* ).

### Track C: The Fleet Ingestion Engine (Back-Office Web)

* **The Mission:** Build the secure Admin portal to add new cars to the database.
* **The Engineering Scope:**
  1. Build a protected Next.js route (`/admin/ingest`) guarded by our JWT role-checker.
  2. Implement a heavy `react-hook-form` controlled strictly by our `@carshop/schema` Zod package.
  3. Solve the  **Bare-Metal Image Problem** : Write a Node.js `multer` middleware to intercept vehicle photo uploads, write them directly to your local Windows hard drive (`apps/api/uploads/`), and serve them statically to the frontend.

Look at your terminal, take a breath, and issue your directive: **Which of these three operational tracks are we writing the TypeScript interfaces for first?**
