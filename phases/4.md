Understood, Mr. Cherkaoui. Bare-metal database synchronization confirmed.

Since the database is now live but completely empty, let's inject a quick seeding script so our frontend actually has data to render. Then, we immediately construct the visual surface:  **The Next.js Web Storefront (`apps/web`)** .

# EXECUTION ORDER 04: Next.js Command Center

### Task 1: Seed the Bare-Metal Database

Let's populate your local PostgreSQL with some high-end inventory. Create `apps/api/src/seed.ts`:

**TypeScript**

```
// apps/api/src/seed.ts
import { PrismaClient } from '@carshop/db';

const prisma = new PrismaClient();

async function main() {
  const hq = await prisma.dealershipLocation.upsert({
    where: { code: 'HQ-RABAT' },
    update: {},
    create: { code: 'HQ-RABAT', name: 'Aurora Premium Auto', address: 'Avenue Mohammed V', city: 'Rabat', phone: '+212500000000' }
  });

  await prisma.vehicleInventory.createMany({
    skipDuplicates: true,
    data: [
      { vin: 'WBA00000000000001', make: 'BMW', model: 'M4 Competition', year: 2026, price: 950000.00, exteriorColor: 'Isle of Man Green', dealershipLocationId: hq.id },
      { vin: 'WP000000000000002', make: 'Porsche', model: '911 GT3', year: 2025, price: 2100000.00, exteriorColor: 'Shark Blue', dealershipLocationId: hq.id },
      { vin: '1HG00000000000003', make: 'Honda', model: 'Civic Type R', year: 2026, price: 550000.00, exteriorColor: 'Championship White', dealershipLocationId: hq.id }
    ]
  });
  console.log('[SEED_COMPLETE]: Test inventory injected into PostgreSQL.');
}

main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
```

Run this script from your workspace root to populate the database:

**Bash**

```
pnpm dlx tsx apps/api/src/seed.ts
```

### Task 2: Build the Frontend Packages

If you haven't already created the internal frontend packages from my previous transmission, build them now:

**1. Design Tokens (`packages/design-tokens/package.json` & `index.ts`):**

**JSON**

```
{ "name": "@carshop/design-tokens", "version": "0.1.0", "private": true, "main": "./index.ts", "types": "./index.ts" }
```

**TypeScript**

```
// packages/design-tokens/index.ts
export const colors = { primary: "#0f172a", secondary: "#3b82f6", accent: "#f59e0b", background: "#f8fafc", surface: "#ffffff", text: "#0f172a", error: "#ef4444", success: "#10b981" };
```

**2. Shared API Client (`packages/api-client/package.json` & `src/index.ts`):**

**JSON**

```
{ "name": "@carshop/api-client", "version": "0.1.0", "private": true, "main": "./src/index.ts", "types": "./src/index.ts", "dependencies": { "@carshop/schema": "workspace:*", "axios": "^1.6.7" } }
```

**TypeScript**

```
// packages/api-client/src/index.ts
import axios from 'axios';
import { VehicleDTO } from '@carshop/schema';

export const apiClient = axios.create({ baseURL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000/api/v1', withCredentials: true });

export const fetchCatalog = async (filters?: any): Promise<VehicleDTO[]> => {
  const response = await apiClient.get('/inventory', { params: filters });
  return response.data.data;
};
```

### Task 3: Scaffold Next.js (`apps/web`)

Create the directory structure:

**Bash**

```
mkdir -p apps/web/src/app
```

Create `apps/web/package.json`:

**JSON**

```
{
  "name": "@carshop/web",
  "version": "0.1.0",
  "private": true,
  "scripts": { "dev": "next dev --port 3000", "build": "next build", "start": "next start" },
  "dependencies": {
    "@carshop/api-client": "workspace:*",
    "@carshop/design-tokens": "workspace:*",
    "@carshop/schema": "workspace:*",
    "next": "14.1.0", "react": "^18.2.0", "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24", "@types/react": "^18.2.61", "autoprefixer": "^10.4.18", "postcss": "^8.4.35", "tailwindcss": "^3.4.1", "typescript": "^5.3.3"
  }
}
```

Create `apps/web/tailwind.config.js`:

**JavaScript**

```
const { colors } = require('@carshop/design-tokens');
module.exports = { content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"], theme: { extend: { colors: { ...colors } } }, plugins: [] };
```

Create `apps/web/postcss.config.js`:

**JavaScript**

```
module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } };
```

Create `apps/web/next.config.js`:

**JavaScript**

```
/** @type {import('next').Config} */
module.exports = { transpilePackages: ["@carshop/api-client", "@carshop/design-tokens", "@carshop/schema"] };
```

Create `apps/web/tsconfig.json`:

**JSON**

```
{
  "compilerOptions": { "target": "es5", "lib": ["dom", "dom.iterable", "esnext"], "allowJs": true, "skipLibCheck": true, "strict": true, "noEmit": true, "esModuleInterop": true, "module": "esnext", "moduleResolution": "bundler", "resolveJsonModule": true, "isolatedModules": true, "jsx": "preserve", "plugins": [{ "name": "next" }], "paths": { "@/*": ["./src/*"] } },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### Task 4: The UI Layer

Create `apps/web/src/app/globals.css`:

**CSS**

```
@tailwind base;
@tailwind components;
@tailwind utilities;

body { background-color: var(--background); color: var(--text); }
```

Create `apps/web/src/app/layout.tsx`:

**TypeScript**

```
import "./globals.css";
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body className="min-h-screen bg-slate-50 antialiased">{children}</body></html>;
}
```

Create `apps/web/src/app/page.tsx`:

**TypeScript**

```
import { fetchCatalog } from '@carshop/api-client';

export default async function HomePage() {
  const inventory = await fetchCatalog().catch(() => []);

  return (
    <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div className="border-b border-slate-200 pb-5 mb-8 flex justify-between items-end">
        <div>
          <h1 className="text-3xl font-bold tracking-tight text-slate-900">Aurora Command Center</h1>
          <p className="text-slate-500 mt-1">Live Relational Vehicle Terminal</p>
        </div>
        <span className="text-xs font-mono bg-blue-100 text-blue-700 font-semibold px-3 py-1 rounded-full border border-blue-200">SYSTEM ONLINE</span>
      </div>

      <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {inventory.length === 0 ? (
          <div className="col-span-full p-12 text-center rounded-2xl border-2 border-dashed border-slate-300 bg-white">
            <p className="text-red-500 font-mono text-sm">[NO_DATA_RETRIEVED]: Verify API Gateway is running.</p>
          </div>
        ) : (
          inventory.map((vehicle) => (
            <div key={vehicle.id} className="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden flex flex-col justify-between">
              <div className="p-6">
                <div className="flex items-center justify-between">
                  <span className="text-xs font-mono text-slate-500 bg-slate-100 px-2 py-0.5 rounded">{vehicle.vin}</span>
                  <span className="text-xs font-bold tracking-wider uppercase px-2 py-0.5 rounded bg-green-100 text-green-700">{vehicle.status}</span>
                </div>
                <h2 className="text-xl font-bold text-slate-900 mt-3">{vehicle.year} {vehicle.make} {vehicle.model}</h2>
                <p className="text-slate-500 text-sm mt-1 capitalize">{vehicle.exteriorColor} • {vehicle.mileage.toLocaleString()} km</p>
              </div>
              <div className="px-6 py-4 bg-slate-50 border-t border-slate-100 flex items-center justify-between">
                <span className="text-lg font-bold text-slate-900">{Number(vehicle.price).toLocaleString('en-US', { style: 'currency', currency: 'MAD' })}</span>
                <button className="bg-blue-600 hover:bg-blue-700 text-white text-xs font-bold px-4 py-2 rounded-lg transition shadow-sm">RESERVE</button>
              </div>
            </div>
          ))
        )}
      </div>
    </main>
  );
}
```

### Task 5: Bind & Boot

Return to your absolute workspace root and run:

**Bash**

```
pnpm install
```

### Checkpoint 04 Verification

1. Ensure the API is running in terminal 1: `pnpm --filter @carshop/api dev`
2. Start Next.js in terminal 2: `pnpm --filter @carshop/web dev`
3. Open `http://localhost:3000` in your browser.

**Report status:** Do you see the BMW M4, Porsche 911, and Honda Civic rendering on the screen? If yes, we initiate the final deployment: Phase 5 (Mobile App). Standing by!
