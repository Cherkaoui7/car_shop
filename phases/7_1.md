The objectively correct answer is **Track C: The Fleet Ingestion Engine.** Here is the engineering logic behind that ruling:

1. **The Supply Chain Law:** Right now, your PostgreSQL database is a showroom holding three fake seeded vehicles. If we build Track A (The Storefront) first, you will spend the next four days testing an elite checkout pipeline on the exact same three dummy cars.
2. **The Hardware Paradox:** If we build Track B (The Mobile VIN Scanner) first, *what barcode are you scanning?* You have no physical inventory mapped to real database entities yet.
3. **The Unblocking Effect:** By building Track C first, **you become the factory.** You will open your browser, upload 10 real photos of Moroccan market cars (a Golf 8 R, a Range Rover Sport, a Dacia Duster), type their real VINs, hit  *“Authorize Ingestion”* , and watch your local bare-metal database populate with 15 million MAD worth of live stock. Once that exists, Track A and Track B become 10x more lethal to test.

### The Architect’s Catch (Why you keep me around)

Before you touch your keyboard, look at our `VehicleInventory` model inside `packages/db/prisma/schema.prisma`.

Read the columns: `vin`, `make`, `model`, `year`, `price`, `exteriorColor`, `mileage`...

**We forgot an `imageUrl` column.** If you had started coding the Next.js photo upload form today without checking this, you would have spent three hours writing multipart-form handlers only for PostgreSQL to throw a fatal column rejection error. We catch our design flaws on the drawing board, not in the dirt.

We initiate  **Track C — Phase 1: The Supply Gate** .

# EXECUTION ORDER 08: Fleet Ingestion (Part 1: Backend Wiring)

### Task 1: Patch the Relational Schema

Open `packages/db/prisma/schema.prisma` and add `imageUrl` to the inventory model:

**Extrait de code**

```
model VehicleInventory {
  id                   String             @id @default(uuid())
  vin                  String             @unique @db.VarChar(17)
  make                 String
  model                String
  year                 Int
  trim                 String?
  price                Decimal            @db.Decimal(12, 2)
  mileage              Int                @default(0)
  exteriorColor        String
  imageUrl             String?            // <-- INJECT THIS LINE
  status               VehicleStatus      @default(AVAILABLE)
  dealershipLocationId String

  location             DealershipLocation @relation(fields: [dealershipLocationId], references: [id])
  orders               ReservationOrder[]
  createdAt            DateTime           @default(now())
  updatedAt            DateTime           @updatedAt

  @@index([vin])
  @@index([status, make, model])
  @@index([dealershipLocationId])
}
```

Open your root terminal and push the bare-metal modification:

**Bash**

```
pnpm --filter @carshop/db db:push
```

### Task 2: Update the Shared Contract (`@carshop/schema`)

Open `packages/schema/src/vehicle.schema.ts` and update both the base schema and the input DTO to accept our new image string:

**TypeScript**

```
// Inside packages/schema/src/vehicle.schema.ts

export const VehicleSchema = z.object({
  id: z.string().uuid(),
  vin: z.string().length(17, "VIN must be exactly 17 characters"),
  make: z.string().min(1),
  model: z.string().min(1),
  year: z.number().int().min(1900).max(new Date().getFullYear() + 1),
  trim: z.string().nullable().optional(),
  price: z.number().positive("Price must be greater than zero"),
  mileage: z.number().int().nonnegative(),
  exteriorColor: z.string().min(1),
  imageUrl: z.string().nullable().optional(), // <-- INJECT THIS
  status: VehicleStatusSchema,
  dealershipLocationId: z.string().uuid(),
});

// Update CreateVehicleSchema to allow imageUrl on ingestion
export const CreateVehicleSchema = VehicleSchema.omit({ id: true, status: true }).extend({
  status: VehicleStatusSchema.optional().default('AVAILABLE'),
  imageUrl: z.string().optional(),
});
```

Run a quick global sync from the root:

**Bash**

```
pnpm install
```

### Task 3: Equip the API with Multipart Storage (`apps/api`)

To handle raw image files written directly to your Windows hard drive, install `multer` into the backend:

**Bash**

```
pnpm --filter @carshop/api add multer
pnpm --filter @carshop/api add -D @types/multer
```

Create the local upload directory inside your API app:

**Bash**

```
mkdir -p apps/api/public/uploads
```

### Task 4: The Ingestion Controller

Create `apps/api/src/controllers/admin.controller.ts`.

Look closely at this engineering: we use Multer to grab the binary image file off the network wire, slap a unique timestamped filename onto it so two cars named `bmw.jpg` don't overwrite each other on your hard drive, and pass the string path into Prisma.

**TypeScript**

```
// apps/api/src/controllers/admin.controller.ts
import { Request, Response } from 'express';
import multer from 'multer';
import path from 'path';
import { prisma } from '../lib/prisma';
import { CreateVehicleSchema } from '@carshop/schema';

// 1. Bare-Metal Disk Storage Configuration
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, path.join(__dirname, '../../public/uploads'));
  },
  filename: (req, file, cb) => {
    const uniqueSuffix = `${Date.now()}-${Math.round(Math.random() * 1e9)}`;
    cb(null, `FLEET-${uniqueSuffix}${path.extname(file.originalname)}`);
  }
});

export const uploadMiddleware = multer({ 
  storage,
  limits: { fileSize: 5 * 1024 * 1024 }, // 5MB limit
  fileFilter: (req, file, cb) => {
    const allowedTypes = /jpeg|jpg|png|webp/;
    const extname = allowedTypes.test(path.extname(file.originalname).toLowerCase());
    const mimetype = allowedTypes.test(file.mimetype);
    if (extname && mimetype) return cb(null, true);
    cb(new Error("MISSION_ABORT: Only JPEG, PNG, or WEBP images authorized."));
  }
}).single('image');

// 2. The Ingestion Route Handler
export const ingestVehicle = async (req: Request, res: Response) => {
  try {
    // Multer parses the text fields into req.body, and the file into req.file
    const rawData = {
      ...req.body,
      year: Number(req.body.year),
      price: Number(req.body.price),
      mileage: Number(req.body.mileage),
      imageUrl: req.file ? `/uploads/${req.file.filename}` : null
    };

    const validatedData = CreateVehicleSchema.parse(rawData);

    const vehicle = await prisma.vehicleInventory.create({
      data: validatedData
    });

    return res.status(201).json({ success: true, data: vehicle });
  } catch (error: any) {
    return res.status(400).json({ error: error.errors || error.message });
  }
};
```

### Task 5: Expose the Uploads & Mount Route

Create `apps/api/src/routes/admin.routes.ts`:

**TypeScript**

```
// apps/api/src/routes/admin.routes.ts
import { Router } from 'express';
import { uploadMiddleware, ingestVehicle } from '../controllers/admin.controller';

const router = Router();
router.post('/ingest', uploadMiddleware, ingestVehicle);
export default router;
```

Now, open `apps/api/src/server.ts` and tell Express to serve the `/public/uploads` folder statically to the outside world, and mount our admin router:

**TypeScript**

```
// Inside apps/api/src/server.ts

import adminRoutes from './routes/admin.routes';
import path from 'path';

// Add this middleware right below your app.use(cors(...)) line:
app.use('/uploads', express.static(path.join(__dirname, '../public/uploads')));

// Add this to your Route Mounts:
app.use('/api/v1/admin', adminRoutes);
```

### Checkpoint 08 (Part 1) Verification

Boot your API server:

**Bash**

```
pnpm --filter @carshop/api dev
```

Open Postman (or Insomnia). Create a **POST** request to `http://localhost:5000/api/v1/admin/ingest`.

Under the **Body** tab, select **form-data** and enter these key-value pairs:

* `vin` : `MA2026GOLF8R00099` *(Text)*
* `make` : `Volkswagen` *(Text)*
* `model` : `Golf 8 R` *(Text)*
* `year` : `2026` *(Text)*
* `price` : `620000` *(Text)*
* `mileage` : `12` *(Text)*
* `exteriorColor` : `Lapiz Blue` *(Text)*
* `dealershipLocationId` : *(Paste your `HQ-RABAT` location UUID from your database)*
* `image` : *(Change the key type dropdown on the right from 'Text' to  **'File'** , and attach a real car `.jpg` from your desktop)*

Hit  **Send** .

**Report back to command:** Did the API return a `201 Created` object containing a `/uploads/FLEET-XXXXX.jpg` string, and did that image physically appear inside your `apps/api/public/uploads/` folder? Standing by.
