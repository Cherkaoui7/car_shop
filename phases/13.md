
Monsieur Cherkaoui, as your Lead Architect, I do not let my developers chase shiny objects. We follow the ironclad hierarchy of software manufacturing.

The objectively superior choice is **Track 2: The Clearing House (The Asynchronous Webhook State Machine).**

Here is the brutal engineering reality behind that ruling:

1. **The Distributed Lie:** Right now, your database flips a vehicle to `PENDING_RESERVATION` the exact millisecond a user clicks a button. In the real world—especially across Moroccan banking gateways like the *Centre Monétique Interbancaire (CMI)* or international Visa/Mastercard rails—a 3D-Secure transaction takes between 4 and 12 seconds to negotiate, clear the acquiring bank, and settle. If your buyer closes their browser tab at second 5, **who owns your Golf 8 R?** You have locked a 620,000 MAD asset to a ghost.
2. **The Concurrency Poison:** If we had built Track 1 (WebSockets) first, your server would be broadcasting *unverified, optimistic lies* to the Lot Manager's phone. They would grab the physical keys to prep a car for a buyer whose credit card is currently bouncing for insufficient funds.
3. **The Staff Engineer Flex:** Writing an  **Idempotent, Cryptographically Signed Webhook Receiver** . This is the exact architectural pattern that separates standard web developers from Principal Engineers.

We are not going to use a mock Stripe API key. We are going to build our own **In-Memory Central Bank Daemon** inside Node.js.

### The Architect’s Warning (The Raw Buffer Trap)

When a real bank (or Stripe) sends a webhook to your server, they sign the raw HTTP byte-stream using an HMAC-SHA256 hash.

If your Express server runs standard `app.use(express.json())` *before* the webhook hits your controller, Express parses the raw bytes into a JavaScript object. When you call `JSON.stringify(req.body)` to check the signature, Node rearranges the object keys or alters the whitespace.  **The cryptographic hash fails 100% of the time** , and your server rejects genuine bank deposits.

We bypass this trap right now by mounting a dedicated **Raw Buffer Route** before global middlewares touch it.

# EXECUTION ORDER 13: The Asynchronous Clearing House

## PHASE A: The Relational State Machine (`packages/db`)

### Task 1: Patch the Enums & Order Manifest

Open `packages/db/prisma/schema.prisma` and expand `OrderStatus` to represent an asynchronous lifecycle:

**Extrait de code**

```
enum OrderStatus {
  PENDING_GATEWAY       // <-- NEW: Intent issued, awaiting bank confirmation
  DEPOSIT_SECURED       // <-- Ratified by Webhook
  FINANCING_APPROVED
  COMPLETED
  CANCELLED
  GATEWAY_REJECTED      // <-- NEW: Wire failed / Card declined
  REFUNDED
}

model ReservationOrder {
  id              String           @id @default(uuid())
  orderNumber     String           @unique
  userId          String
  vehicleId       String
  status          OrderStatus      @default(PENDING_GATEWAY) // Changed default
  depositAmount   Decimal          @db.Decimal(10, 2)
  finalPrice      Decimal          @db.Decimal(12, 2)
  intentToken     String           @unique // Cryptographic tracking ticket
  expiresAt       DateTime       
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt

  user            User             @relation(fields: [userId], references: [id])
  vehicle         VehicleInventory @relation(fields: [vehicleId], references: [id])

  @@index([orderNumber])
  @@index([intentToken])
  @@index([userId, status])
  @@index([vehicleId, status])
}
```

Push the state update to your local Postgres bare metal:

**Bash**

```
pnpm --filter @carshop/db db:push
```

## PHASE B: The Central Bank Robot (`apps/api`)

We will program a background worker that intercepts your buyer's intent, sleeps for 6 seconds to simulate interbank network lag, cryptographically signs a settlement payload, and fires it back at your own API.

### Task 2: Inject the Cryptographic Secret

Open `apps/api/.env` and append:

**Extrait de code**

```
BANK_WEBHOOK_SECRET="AURORA_CENTRAL_BANK_HMAC_KEY_9942_!#"
```

### Task 3: Author the Bank Simulator

Create `apps/api/src/lib/mock-bank.ts`:

**TypeScript**

```
// apps/api/src/lib/mock-bank.ts
import crypto from 'crypto';

export const dispatchSyntheticBankClearing = (intentToken: string, orderNumber: string) => {
  // Simulate 6-second interbank settlement latency
  setTimeout(async () => {
    const payload = JSON.stringify({
      event: 'TRANSACTION_SETTLED_SUCCESS',
      intentToken,
      orderNumber,
      timestamp: new Date().toISOString(),
    });

    // Forge Bank HMAC-SHA256 Signature
    const secret = process.env.BANK_WEBHOOK_SECRET!;
    const signature = crypto.createHmac('sha256', secret).update(payload).digest('hex');

    console.log(`[CENTRAL BANK DAEMON]: Wire settled for ${orderNumber}. Dispatching signed Webhook...`);

    try {
      await fetch('http://localhost:5000/api/v1/webhooks/bank-settlement', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'x-aurora-bank-signature': signature,
        },
        body: payload,
      });
    } catch (e: any) {
      console.error(`[BANK DAEMON PANIC]: Failed to deliver settlement packet: ${e.message}`);
    }
  }, 6000);
};
```

## PHASE C: Refactoring the Booking Core (`apps/api`)

### Task 4: Upgrade `reserveVehicle`

Open `apps/api/src/controllers/inventory.controller.ts`.

Look at the logic change: we issue the order as `PENDING_GATEWAY`, generate a unique `intentToken`,  **dispatch the bank robot into the background** , and immediately release the HTTP thread back to Next.js.

**TypeScript**

```
// Inside apps/api/src/controllers/inventory.controller.ts
import crypto from 'crypto';
import { dispatchSyntheticBankClearing } from '../lib/mock-bank';

export const reserveVehicle = async (req: Request, res: Response) => {
  try {
    const { vehicleId, userId, depositAmount } = req.body;

    const intentToken = `INTENT-${crypto.randomBytes(16).toString('hex').toUpperCase()}`;
    const orderNum = `ORD-${Math.floor(100000 + Math.random() * 900000)}`;

    const order = await prisma.$transaction(async (tx) => {
      const targetVehicle = await tx.vehicleInventory.findUnique({ where: { id: vehicleId } });

      if (!targetVehicle || targetVehicle.status !== 'AVAILABLE') {
        throw new Error("VEHICLE_UNAVAILABLE_FOR_RESERVATION");
      }

      // Optimistic Mutex Lock
      await tx.vehicleInventory.update({
        where: { id: vehicleId },
        data: { status: 'PENDING_RESERVATION' }
      });

      return await tx.reservationOrder.create({
        data: {
          orderNumber: orderNum,
          userId,
          vehicleId,
          depositAmount,
          finalPrice: targetVehicle.price,
          intentToken,
          status: 'PENDING_GATEWAY',
          expiresAt: new Date(Date.now() + 48 * 60 * 60 * 1000),
        }
      });
    });

    // Unleash the asynchronous clearing house robot
    dispatchSyntheticBankClearing(intentToken, orderNum);

    return res.status(200).json({ success: true, order });
  } catch (error: any) {
    return res.status(409).json({ error: error.message });
  }
};
```

## PHASE D: The Cryptographic Webhook Receiver (`apps/api`)

### Task 5: Author the Idempotent Receiver

Create `apps/api/src/controllers/webhook.controller.ts`.

Look at the double-security here: we verify the HMAC signature against the raw buffer, and then we check `if (order.status === 'DEPOSIT_SECURED') return;` so that if the bank accidentally transmits the webhook twice, we don't process the state twice.

**TypeScript**

```
// apps/api/src/controllers/webhook.controller.ts
import { Request, Response } from 'express';
import crypto from 'crypto';
import { prisma } from '../lib/prisma';

export const handleBankSettlement = async (req: Request, res: Response) => {
  try {
    const incomingSignature = req.headers['x-aurora-bank-signature'];
    if (!incomingSignature || typeof incomingSignature !== 'string') {
      return res.status(401).json({ error: "MISSING_CRYPTOGRAPHIC_BANK_SIGNATURE" });
    }

    // req.body is preserved as a raw Buffer by our specialized route mount
    const rawBodyBuffer = req.body as Buffer;
    const secret = process.env.BANK_WEBHOOK_SECRET!;
  
    const computedSignature = crypto
      .createHmac('sha256', secret)
      .update(rawBodyBuffer)
      .digest('hex');

    if (!crypto.timingSafeEqual(Buffer.from(incomingSignature), Buffer.from(computedSignature))) {
      console.error("[SECURITY ALERT]: Forged Webhook signature intercepted and dropped!");
      return res.status(403).json({ error: "HMAC_SIGNATURE_MISMATCH" });
    }

    const eventData = JSON.parse(rawBodyBuffer.toString('utf8'));
    const { intentToken, event } = eventData;

    if (event !== 'TRANSACTION_SETTLED_SUCCESS') {
      return res.status(200).json({ status: "IGNORED_NON_SETTLEMENT_EVENT" });
    }

    // Idempotent State Ratification
    await prisma.$transaction(async (tx) => {
      const order = await tx.reservationOrder.findUnique({ where: { intentToken } });
      if (!order) throw new Error("UNINDEXED_INTENT_TOKEN");

      // IDEMPOTENCY GUARD: If already secured, silently acknowledge to stop bank retries
      if (order.status === 'DEPOSIT_SECURED') return;

      await tx.reservationOrder.update({
        where: { intentToken },
        data: { status: 'DEPOSIT_SECURED' }
      });

      console.log(`[RATIFICATION COMPLETE]: Ledger Mutex officially ratified for ${order.orderNumber}`);
    });

    return res.status(200).json({ received: true });
  } catch (error: any) {
    console.error(`[WEBHOOK PANIC]: ${error.message}`);
    return res.status(500).json({ error: error.message });
  }
};
```

### Task 6: Mount the Raw Buffer Bypass

Open `apps/api/src/server.ts`.  **Surgically inject this route before `app.use(express.json())`** :

**TypeScript**

```
// Inside apps/api/src/server.ts

import { handleBankSettlement } from './controllers/webhook.controller';

const app = express();
const PORT = process.env.PORT || 5000;

// =========================================================================
// CRITICAL BYPASS: Mount Webhook raw parser BEFORE express.json() mounts!
// =========================================================================
app.post(
  '/api/v1/webhooks/bank-settlement',
  express.raw({ type: 'application/json' }),
  handleBankSettlement
);

// Standard Middlewares
app.use(express.json());
// ... keep the rest of your server.ts unchanged ...
```

### Checkpoint 13 Verification

Recompile and boot the master matrix:

**Bash**

```
pnpm build && pnpm start
```

1. Open your browser to `http://localhost:3000` and click **`AUTHORIZE 10% HOLD`** on an available vehicle.
2. The UI will instantly lock into the ratified state.
3. **Do not click anything.** Keep your eyes glued to Terminal 1 (your API stdout).

Count to six out loud.

At second six, verify that your terminal explodes with these exact two consecutive telemetry logs:

**Plaintext**

```
[CENTRAL BANK DAEMON]: Wire settled for ORD-XXXXXX. Dispatching signed Webhook...
[RATIFICATION COMPLETE]: Ledger Mutex officially ratified for ORD-XXXXXX
```
